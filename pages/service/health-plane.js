import React from 'react';
import withAuth from '../withAuth/withAuth';

const HealthPlane = () => {
    return (
        <div>
            This is health plaane
            
        </div>
    );
};

export default withAuth(HealthPlane);